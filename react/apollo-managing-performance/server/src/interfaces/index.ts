export type Speaker = {
  bio: string;
  company: string;
  favorite: boolean;
  first: string;
  id: string;
  last: string;
  twitterHandle: string;
};

export type SpeakerInput = {
  favorite: boolean;
  first: string;
  last: string;
};
